  <!-- Main Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">

      <!-- Brand Content -->
      <a class="navbar-brand" href="/">
        <img src="/Assets/images/logo.png" alt="Logo" width="40" height="40">
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

          <li class="nav-item">
            <% if (title == "Home") { %> 
            <a class="nav-link active" aria-current="page" href="/home"><i class="fas fa-h-square"></i> Home</a>
            <% } else { %>
              <a class="nav-link" href="/home"><i class="fas fa-h-square"></i> Home</a>
            <% } %>  
          </li>

          <li class="nav-item">
            <% if (title == "About") { %> 
              <a class="nav-link active" aria-current="page" href="/about"><i class="fas fa-address-card"></i> About</a>
              <% } else { %>
                <a class="nav-link" href="/about"><i class="fas fa-address-card"></i> About</a>
              <% } %>  
          </li>
          
          <li class="nav-item">
            <% if (title == "Projects") { %> 
              <a class="nav-link active" aria-current="page" href="/projects"><i class="fas fa-project-diagram"></i> Projects</a>
              <% } else { %>
                <a class="nav-link" href="/projects"><i class="fas fa-project-diagram"></i> Projects</a>
              <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "Services") { %> 
              <a class="nav-link active" aria-current="page" href="/services"><i class="fas fa-concierge-bell"></i></i> Services</a>
              <% } else { %>
                <a class="nav-link" href="/services"><i class="fas fa-concierge-bell"></i> Services</a>
              <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "Contacts") { %> 
              <a class="nav-link active" aria-current="page" href="/contact-list"><i class="fas fa-briefcase"></i> Business Contacts</a>
              <% } else { %>
                <a class="nav-link" href="/contact-list"><i class="fas fa-briefcase"></i> Business Contacts</a>
              <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "Contact") { %> 
              <a class="nav-link active" aria-current="page" href="/contact"><i class="fas fa-mobile-alt"></i> Contact</a>
              <% } else { %>
                <a class="nav-link" href="/contactMe"><i class="fas fa-mobile-alt"></i> Contact</a>
              <% } %>
          </li>
          
          <% if(typeof displayName == "string" && displayName) { %>
            <li class="navbar-text">
              <i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %>
            </li>
            <li class="nav-item">
              <a href="/logout" class="nav-link"
                ><i class="fas fa-lg fa-sign-out-alt"></i> Logout</a
              >
            </li>
            <% } else { %>
            <li clas="nav-item">
              <a href="/login" class="nav-link"
                ><i class="fas fa-lg fa-sign-in-alt"></i> Login</a
              >
            </li>
            <% } %>
        </ul>
      </div>
    </div>
  </nav>